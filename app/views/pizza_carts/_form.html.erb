<%= form_with(model: pizza_cart, local: true) do |form| %>
  <% if pizza_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pizza_cart.errors.count, "error") %> prohibited this pizza_cart from being saved:</h2>

      <ul>
      <% pizza_cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cart %>
    <%= form.text_field :cart %>
  </div>

  <div class="field">
    <%= form.label :pizza_id %>
    <%= form.text_field :pizza_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
