<%= form_with(model: ingredient_recipe, local: true) do |form| %>
  <% if ingredient_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredient_recipe.errors.count, "error") %> prohibited this ingredient_recipe from being saved:</h2>

      <ul>
      <% ingredient_recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :recipe_id %>
    <%= form.text_field :recipe_id %>
  </div>

  <div class="field">
    <%= form.label :ingredient_id %>
    <%= form.text_field :ingredient_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
